// ---------- 引用 ----------------------------------------------------------------
// ---------- 常數 ----------------------------------------------------------------
// ---------- 成員變數 -------------------------------------------------------------
// ---------- 生命週期 -------------------------------------------------------------
// ---------- 框架呼叫 -------------------------------------------------------------
// ---------- 內部呼叫 -------------------------------------------------------------
// ---------- 外部部呼叫 -----------------------------------------------------------



// ---------- 引用 ----------------------------------------------------------------
import { _decorator, Component, Node } from 'cc';
const { ccclass, property } = _decorator;

// ---------- 常數 ----------------------------------------------------------------
@ccclass('<%Name%>')
export class <%Name%> extends Component {
    // ---------- 聲明變數 --------------------------------------------------------
    @property(Node)
    public someNode:Node = null;

    // ---------- 成員變數 --------------------------------------------------------

    // ---------- 生命週期 --------------------------------------------------------
    start () {
        this.init();
    }

    onDisable () { }

    // ---------- 內部呼叫 --------------------------------------------------------
    private init () { }

    // ---------- 外部部呼叫 ------------------------------------------------------
    
    

}




/**
 * ********* View *********
    // ---------- 生命週期 --------------------------------------------------------
    onLoad () {
        this.node.addComponent(xxxController);
    }

 * ********* Controller *********
    // ---------- 成員變數 --------------------------------------------------------
    private _view: xxxView = null!;
    
    // ---------- 生命週期 --------------------------------------------------------
    onLoad () {
        this._view = this.node.getComponent(xxxView);
        this.addEvents();
    }
    start () {
        this.init();
    }

    // ---------- 監聽事件 --------------------------------------------------------
    private addEvents (): void { }
 */