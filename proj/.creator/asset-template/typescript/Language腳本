// ---------- 引用 ----------------------------------------------------------------
import { LanguageDelegate, injectLanguageData } from "../../../../scripts/framework/core/language/LanguageDelegate";
import * as languages from "./<%Name%>s";

// ---------- 常數 ----------------------------------------------------------------


@injectLanguageData
export class <%Name%> extends LanguageDelegate {

    // ---------- 成員變數 -------------------------------------------------------------
    // ---------- 生命週期 -------------------------------------------------------------
    // ---------- 框架呼叫 -------------------------------------------------------------
    // ---------- 內部呼叫 -------------------------------------------------------------
    // ---------- 外部部呼叫 -----------------------------------------------------------

    init(): void {
        this.importLanguages(languages);
    }
    bundle = EBundles[EBundles.unknown];
}